装袋（bagging）算法：先将训练集分离成多个子集，然后通过各个子集训练多个模型。

#装袋决策树(bagged decision trees)
#随机森林(random forest)
#极端随机树(Extra Trees)

提升（boosting）算法：训练多个模型并组成一个序列，序列中的每一个模型会修正前一个模型的错误。

#Adaboost
#随机梯度提升(Stochastic Gradient Boosting)


投票（voting）算法：训练多个模型，并采用样本统计来提高模型的准确度。
